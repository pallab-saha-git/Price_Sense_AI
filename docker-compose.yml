version: '3.9'

services:
  pricesense:
    build: .
    ports:
      - "8050:8050"
    volumes:
      - ./data:/app/data
    environment:
      - DATABASE_URL=sqlite:///./data/pricesense.db
      - DEBUG=false
      - HOST=0.0.0.0
    env_file:
      - .env
    restart: unless-stopped
